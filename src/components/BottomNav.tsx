export default function BottomNav({tab,setTab}:{tab:string;setTab:(t:'board'|'add'|'notes')=>void}){return <nav className='fixed bottom-0 left-0 right-0 bg-white border-t p-2 flex gap-2 max-w-md mx-auto'>{[['board','Board'],['add','Add'],['notes','Field Notes']].map(([k,l])=><button key={k} className={`flex-1 ${tab===k?'bg-teal-600 text-white':'border'}`} onClick={()=>setTab(k as any)}>{l}</button>)}</nav>}

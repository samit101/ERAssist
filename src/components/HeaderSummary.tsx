export default function HeaderSummary({seen,target,setSeen,openCases,openThreads,dueNow}:{seen:number;target:number;setSeen:(n:number)=>void;openCases:number;openThreads:number;dueNow:number}){return <div className='card sticky top-0 z-10'><div className='flex justify-between items-center'><div className='font-semibold'>Patients Seen: {seen} / {target}</div><div className='space-x-2'><button className='border' onClick={()=>setSeen(Math.max(0,seen-1))}>-</button><button className='border' onClick={()=>setSeen(seen+1)}>+</button></div></div><div className='text-sm mt-1'>Open Cases: {openCases} · Open Threads: {openThreads} · Due Now: {dueNow}</div></div>}

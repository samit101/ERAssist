export default function HeaderSummary({seen,target,setSeen,threadsDue}:{seen:number;target:number;setSeen:(n:number)=>void;threadsDue:number}){return <div className='card sticky top-0 z-10'><div className='flex justify-between items-center'><div className='font-semibold'>Patients Seen: {seen} / {target}</div><div className='space-x-2'><button className='border' onClick={()=>setSeen(Math.max(0,seen-1))}>-</button><button className='border' onClick={()=>setSeen(seen+1)}>+</button></div></div><div className='text-xs text-slate-600 mt-1'>{seen}/9 = {(seen/9).toFixed(2)}</div><div className='text-xs text-slate-600'>Threads Due: {threadsDue}</div></div>}

import { TaskThread } from '../types';import { relDue,isDue } from '../utils/reminders';
export default function TaskItem({task,onToggle,onSnooze}:{task:TaskThread;onToggle:()=>void;onSnooze:(m:number)=>void}){const due=isDue(task);return <div className={`p-2 rounded-xl border ${due?'border-amber-500 bg-amber-50':'border-slate-200'}`}><label className='flex gap-2 items-start'><input type='checkbox' onChange={onToggle}/><span className='text-sm flex-1'>{task.text}</span></label><div className='text-xs mt-1'>{relDue(task)}{due&&<span className='ml-2 space-x-1'><button className='border text-xs' onClick={()=>onSnooze(5)}>+5</button><button className='border text-xs' onClick={()=>onSnooze(10)}>+10</button><button className='border text-xs' onClick={()=>onSnooze(15)}>+15</button></span>}</div></div>}
